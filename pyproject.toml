[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pixelstacker-cli"
version = "0.1.0"
description = "CLI for PixelStacker"
requires-python = ">=3.10"
authors = [{ name = "Luigi Vannozzi" }]

[project.scripts]
pixelstacker = "stacking.cli:main"